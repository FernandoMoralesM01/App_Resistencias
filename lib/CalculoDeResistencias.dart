import 'package:flutter/material.dart';

class CalculoDeResistencias extends StatelessWidget {

  final int index;

  CalculoDeResistencias(this.index);

  @override
  Widget build(BuildContext context) {
    return  Container(

    );
  }
}
