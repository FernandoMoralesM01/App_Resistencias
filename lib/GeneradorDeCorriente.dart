import 'package:flutter/material.dart';

class GeneradorDeCorriente extends StatelessWidget {

  final int index;

  GeneradorDeCorriente(this.index);

  @override
  Widget build(BuildContext context) {
    return  Container(

    );
  }
}